# Structured v0.2.0 entity examples

These files are synthetic. The IDs are coordinated so they can be added in
numeric order to a project named `demo-manual-002` after Gate 0 has passed.
They illustrate the normalized entity payload format; they do not represent a
real heritage community, authorization, model run, market study, or payment.
